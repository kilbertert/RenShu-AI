# 启动指南 · RenShu-AI（仁术 AI）

> 文档编号：001（启动类）
> 最后更新：2026-06-05

---

## 1. 文档目的

让**任何一位新成员或被中断后回归的开发人员**在 10 分钟内从零把整个项目（数据库 / 后端 / 前端）跑起来。

## 2. 覆盖范围

- 本机（Windows 11 + WSL 可选 / macOS / Linux）从零起步
- 所有依赖：Docker 容器、Python venv、Node modules、`.env` 配置文件
- 前后端启动 / 关闭 / 重启
- 常见故障排查（含"误删镜像/容器"恢复）

## 3. 核心事实（架构摘要）

| 项 | 值 | 来源 |
|---|---|---|
| 后端框架 | FastAPI（async，uvicorn） | `backend/main.py` |
| 后端端口（默认） | `8000`（`main.py` 内） | 但**实际使用 `8091`**，避开本地 ghost socket `8090` |
| 后端端口（实际） | **`8091`** | `backend/main.py:98` 可改 |
| 前端框架 | React 19 + Vite | `frontend/vite.config.*` |
| 前端端口 | **`3000`** | `frontend/vite.config.*: server.port` |
| 前端 → 后端 | `http://localhost:8091` | `frontend/.env.dev` → `VITE_API_BASE_URL` |
| 编排 | Docker Compose（4 服务） | `docker-compose.yml` |
| 数据库 | PostgreSQL 16 | `localhost:5432` |
| 缓存 | Redis 7 | `localhost:6390`（容器内 `6379`） |
| 向量库 | Qdrant | `localhost:6333` (HTTP) / `6334` (gRPC) |
| 图数据库 | Neo4j 5.26.4 + APOC | `localhost:7474` (HTTP) / `7687` (Bolt) |
| 数据卷 | `postgres_data / redis_data / qdrant_data / neo4j_data` | **删除容器/镜像不会丢数据** |

---

## 4. 前置准备（首次必做）

### 4.1 工具链

| 工具 | 推荐版本 | 验证命令 | 说明 |
|---|---|---|---|
| Docker Desktop | ≥ 4.20 | `docker --version` | Windows 必须开启 WSL2 后端 |
| Python | **3.11.7**（项目锁定） | `python --version` | 后端 `pyproject.toml` 要求 |
| uv | ≥ 0.11 | `uv --version` | 项目用 `uv` 管理依赖 |
| Node.js | ≥ 20 | `node --version` | 推荐 LTS 22 |
| npm | ≥ 10 | `npm --version` | 随 Node 装 |

> **重要**：项目根目录 `D:\AI\project\RenShu-AI\backend` 的 `.venv` 已存在，里面是 Python 3.11.7。**不要用系统 Python 3.13** 直接装依赖，否则 SQLAlchemy / psycopg 编译会失败。

### 4.2 确认 Docker 守护进程在跑

```bash
docker info
# 输出中应看到 "Server Version" 和 "Running: true"
```

如果 Windows 上 `docker info` 卡死 → 检查任务栏 Docker Desktop 图标是否常驻（动画表示启动中，常亮表示就绪）。

### 4.3 创建后端 `.env`（项目根）

项目根目前**没有 `.env` 文件**。需要新建一个（被 `.gitignore` 忽略）：

```env
# 后端监听（可选，默认 8000）
APP_HOST=0.0.0.0
APP_PORT=8091

# PostgreSQL（与 docker-compose.yml 一致）
POSTGRESQL_DATABASE_NAME=tcm_agent_db
POSTGRESQL_USER_NAME=postgres
POSTGRESQL_PASSWORD=<set-a-local-password>
POSTGRESQL_HOST=localhost
POSTGRESQL_PORT=5432
POSTGRESQL_POOL_SIZE=20
POSTGRESQL_MAX_OVERFLOW=40
POSTGRESQL_POOL_RECYCLE=3600
POSTGRESQL_ECHO=false
POSTGRESQL_DRIVER_ASYNC=asyncpg
POSTGRESQL_DRIVER_SYNC=psycopg2

# Redis
REDIS_URL=redis://localhost:6390/0
REDIS_ENABLED=true

# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=<set-a-local-password>

# Qdrant
QDRANT_URL=http://localhost:6333

# LLM 提供商（按需填，不填也能启动，调用时才报错）
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
OPENAI_API_KEY=
DASHSCOPE_API_KEY=
LANGSMITH_API_KEY=
LANGSMITH_TRACING=false

# 安全（生成 JWT 用）
JWT_SECRET_KEY=change-me-in-production-2026
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440
```

> 这些字段名严格匹配 `backend/app/src/common/config/setting_config.py` 的 Pydantic Settings 定义，**拼写错一个字母启动会失败**。

### 4.4 创建前端 `.env.dev`（已存在，需确认）

`frontend/.env.dev` 内容（已经维护好）：

```env
VITE_API_BASE_URL=http://localhost:8091
GEMINI_API_KEY=PLACEHOLDER_API_KEY
```

> `VITE_API_BASE_URL` 改了之后**必须 hard refresh**（`Ctrl+Shift+R`）Vite 才会重新加载。

---

## 5. 启动顺序（推荐）

**严格按顺序启动，避免后端先起连不上数据库**：

```
1. Docker 容器（数据库/缓存/向量库/图库）
2. 后端（FastAPI）
3. 前端（Vite）
```

### 5.1 启动 Docker 服务

```bash
# 项目根
cd D:\AI\project\RenShu-AI

# 首次：拉镜像 + 后台启动
docker compose up -d

# 已存在：只启动
docker compose start

# 查看状态（应该看到 4 个容器都是 Up）
docker compose ps
```

预期输出（节选）：

```
NAME           STATUS          PORTS
tcm-postgres   Up (healthy)    0.0.0.0:5432->5432/tcp
tcm-redis      Up (healthy)    0.0.0.0:6390->6379/tcp
tcm-qdrant     Up              0.0.0.0:6333-6334->6333-6334/tcp
tcm-neo4j      Up (healthy)    0.0.0.0:7474->7474/tcp, 0.0.0.0:7687->7687/tcp
```

**健康检查命令**：

```bash
# Windows
curl http://localhost:5432
curl http://localhost:6390
curl http://localhost:7474
curl http://localhost:6333
```

### 5.2 启动后端

```bash
# 项目根
cd D:\AI\project\RenShu-AI\backend

# 首次：装依赖（如果 .venv 已有则跳过）
uv sync

# 启动（uvicorn）
./.venv/Scripts/python.exe -m uvicorn main:app --host 0.0.0.0 --port 8091 --log-level info
```

预期日志关键行：

```
[2026-06-XX] INFO [app] 创建 FastAPI 应用实例
[2026-06-XX] INFO [sql] PostgreSQL异步引擎创建成功
[2026-06-XX] INFO [app] ✅ PostgreSQL 初始化完成
[2026-06-XX] INFO [app] ✅ Redis 缓存初始化完成
INFO:     Uvicorn running on http://0.0.0.0:8091
```

**Swagger UI**：`http://localhost:8091/docs`

### 5.3 启动前端

```bash
# 新开一个终端
cd D:\AI\project\RenShu-AI\frontend

# 首次：装依赖
npm install

# 启动
npm run dev
```

预期输出：

```
  VITE vX.X.X  ready in XXX ms
  ➜  Local:   http://localhost:3000/
```

**访问**：`http://localhost:3000`

---

## 6. 关闭与重启

### 6.1 关闭

| 顺序 | 命令 | 说明 |
|---|---|---|
| 1 | `Ctrl+C` 停前后端终端 | 先停应用 |
| 2 | `docker compose stop` | 停容器（**保留数据**） |
| 3 | `docker compose down` | 停+删容器（**保留数据卷**） |
| 4 | `docker compose down -v` | ⚠️ **连数据卷一起删**（慎用） |

### 6.2 重启

| 场景 | 操作 |
|---|---|
| 只改前端代码 | 前后端都不用动，Vite HMR 自动热更新 |
| 改后端代码 | 重启 uvicorn（`Ctrl+C` 然后再起） |
| 改 `docker-compose.yml` | `docker compose up -d`（会自动 diff 配置） |
| 改 `.env` | 重启后端（uvicorn 启动时加载一次） |
| 改数据库 schema | 见 §7.4 |

---

## 7. 故障排查

### 7.1 后端启动报 `Redis 连接失败: localhost:6390`

**症状**：
```
ERROR [redis_config] ❌ Redis 连接失败: Error 22 connecting to localhost:6390
```

**原因**：`tcm-redis` 容器没起，或者镜像/容器被误删。

**修复**：
```bash
docker ps -a | grep tcm-redis     # 看是否真的没起
docker compose up -d redis        # 只起 Redis
# 或者全部重启
docker compose up -d
```

**重要**：删容器/镜像**不会丢数据**（数据在 `redis_data` 卷里），重新 `up -d` 即可。

### 7.2 前端登录后白屏

**症状**：登录 API 返回 200，但页面停在 `/login/public` 或跳到 `/public` 是空白。

**修复路径**（按概率排序）：

1. **端口错位**：检查 `frontend/.env.dev` 的 `VITE_API_BASE_URL` 是否是 `http://localhost:8091`（不是 `8090`）。改完必须 `Ctrl+Shift+R`。
2. **后端没起来**：`curl http://localhost:8091/docs` 看是否 200。
3. **CORS 错误**：打开浏览器 DevTools → Console，看红色错误。
4. **React 渲染异常**：DevTools Console 第一条红色错误的 stack trace 通常能定位到行号（最常见：`PublicPortal.tsx` 内 2000+ 行那个组件）。

**调试模板**：
```
F12 → Console → 勾上 "Preserve log" → 重现登录 → 把第一张红色错误截图给我
```

### 7.3 端口被占（`Address already in use`）

**症状**：
```
ERROR: [Errno 10048] error while attempting to bind on address ('0.0.0.0', 8091)
```

**修复（Windows）**：
```bash
netstat -ano | findstr :8091
# 找到 LISTENING 那一行的 PID（最右列），比如 12345
taskkill /PID 12345 /F
```

**修复（通用）**：
```bash
# Linux/macOS
lsof -i :8091
kill -9 <PID>
```

### 7.4 数据库表不存在 / migration 报错

**症状**：启动报 `relation "xxx" does not exist`。

**当前状态**：项目**没有**自动 migration 机制（`alembic` 未配置）。`backend/main.py` 启动时会调用 `create_db_tables()` 自动建表，但**只新增不修改**。

**修复**：
```bash
# 进入 backend 目录，用 SQLAlchemy 强制重建（⚠️ 丢数据）
./.venv/Scripts/python.exe -c "from app.src.common.config.prosgresql_config import create_db_tables; create_db_tables()"
```

**正确做法**：找对应的 SQL 脚本，登录 PostgreSQL 手动跑：
```bash
docker exec -it tcm-postgres psql -U postgres -d tcm_agent_db
\dt        # 看现有表
```

### 7.5 Neo4j 启动慢

**症状**：`tcm-neo4j` 容器 Up 了，但 `bolt://localhost:7687` 连不上。

**原因**：Neo4j 第一次启动要 30-90 秒做数据初始化。

**修复**：等。日志命令 `docker logs -f tcm-neo4j`，看到 `Started.` 才是真的就绪。

### 7.6 误删了镜像/容器

**重要**：DOCKER **卷（volumes）独立于容器和镜像存在**。

| 误操作 | 数据是否丢 | 恢复 |
|---|---|---|
| `docker rm tcm-postgres` | ❌ **不丢** | `docker compose up -d` |
| `docker rmi postgres:16-alpine` | ❌ **不丢** | `docker compose pull` 然后 `up -d` |
| `docker system prune` | ❌ **不丢**（默认不删卷） | 同上 |
| `docker volume rm postgres_data` | ✅ **全丢** | 无解，只能重灌 |
| `docker compose down -v` | ✅ **全丢** | 无解 |

**典型恢复流程**（如果删了所有镜像但卷还在）：
```bash
cd D:\AI\project\RenShu-AI
docker compose pull      # 重新拉镜像
docker compose up -d     # 用现有卷重新创建容器
docker compose ps        # 确认都 Up
```

数据**应该都在**（用户名、对话历史、患者档案等）。

### 7.7 Windows 上 `.venv` 路径问题

**症状**：uvicorn 报 `ModuleNotFoundError: No module named 'app'`

**原因**：没在 `backend/` 目录下启动，或 `.venv` 路径不对。

**修复**：
```bash
# 确认当前目录
cd D:\AI\project\RenShu-AI\backend

# 用项目自己的 venv 解释器（不是系统 python）
./.venv/Scripts/python.exe -m uvicorn main:app --host 0.0.0.0 --port 8091
```

---

## 8. 验证清单（启动后必跑）

启动完所有服务后，按顺序确认：

- [ ] `docker compose ps` 显示 4 个容器都 `Up`
- [ ] `curl http://localhost:7474` 返回 HTML（Neo4j 浏览器）
- [ ] `curl http://localhost:8091/docs` 返回 200（Swagger UI）
- [ ] 浏览器打开 `http://localhost:3000` 看到 landing page
- [ ] 点击登录 → 输入测试账号 → 跳到 `/public` 且**有内容**（不是白屏）

如果最后一项失败 → 回 §7.2。

---

## 9. 与其他文档的关联

- **架构总览**：`CLAUDE.md`（项目根）
- **后端模块结构**：`CLAUDE.md` → "Backend Structure"
- **API 端点清单**：`CLAUDE.md` → "API Endpoints"
- **数据库模型**：`backend/app/src/model/medical_models.py`
- **GraphRAG 配置**：`backend/graphrag/`

---

## 10. 当前已知边界

- **没有 Alembic**：schema 变更需手动 `psql` 跑 SQL（或自己写脚本）
- **没有 CI/CD**：所有启动都是手动
- **没有生产部署文档**：本文档只覆盖本地开发
- **没有 `.env` 模板自动生成**：需要参考 §4.3 手动创建
- **StrictMode 已禁用**（`frontend/src/main.tsx`）：注释里说"排查重复请求时临时禁用"

---

## 11. 待确认问题

- [ ] 启动日志里 `[LLM-HTTP-DEBUG] httpx interceptor installed` 是调试用，是否要永久保留？
- [ ] `main.py:98` 的 `port=8000` 是否要改为读取 `APP_PORT` 环境变量（避免命令行覆盖的不一致）？
- [ ] 端口 `8090` 这个"ghost socket"是历史遗留还是被其他项目占用？是否要彻底弃用？

---

## 12. 更新记录

| 日期 | 变更 | 可信度 |
|---|---|---|
| 2026-06-05 | 首次完整化：加入前置/故障排查/误删恢复/验证清单 | 高 |
| 2026-06-04 | 简版：仅列出后端 + 前端启动命令 | 中 |
